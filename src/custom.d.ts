type RecordItem = {
  type: string;
  output: string;
  tagsName: string;
  note: string;
  amount: number;
  createdAt?: Date;
}
type Tag = {
  id: string;
  iconName: string;
  tagText: string;
}

// interface Window {
// }

